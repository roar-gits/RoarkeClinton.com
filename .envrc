# ==============================================================================
# RoarkeClinton.com - direnv configuration
# ==============================================================================
# Automatically loads project-specific credentials when entering this directory.
# Only RoarkeClinton tokens are available - true credential isolation.
#
# Docs: ~/Programming/DevSystems/credential-system/README.md
# ==============================================================================

source_env ~/.env.credentials/roarkeclinton.env

# Export for gh CLI (isolated per-project)
# gh CLI respects GITHUB_TOKEN env var for authentication
export GITHUB_TOKEN="${ROARKECLINTON_GITHUB_TOKEN}"
